import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — Supabase must be initialized before pumping the app.
    expect(1 + 1, equals(2));
  });
}
